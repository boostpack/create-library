const boostpackConfig = require('@boostpack/create-library/commitlint');

// Commitlint configuration
// Extends base configuration from @boostpack/create-library
// You can override or extend these settings as needed

module.exports = {
  ...boostpackConfig,
}
